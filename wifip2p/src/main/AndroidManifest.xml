<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ducnguyen.wifip2p">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <service android:name=".discovery.UdpBroadcastService" />

        <service android:name=".discovery.UdpServerService" />

        <service android:name=".connect.TcpClientService" />

        <service android:name=".connect.TcpServerService" />

    </application>

</manifest>
